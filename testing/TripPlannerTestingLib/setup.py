from setuptools import setup

setup(
    name = 'TripPlannerTestingLib',
    version = '0.1',
    packages = ['tptesting', 'tpacceptance'],
    install_requires = '''
        PyYAML
        '''
    )
        
